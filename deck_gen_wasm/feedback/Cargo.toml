[package]
name = "deck_gen_wasm_feedback"
version.workspace = true
edition.workspace = true
description = "Feedback button mailto composer for the browser workspace editor"

[lib]
path = "src/lib.rs"

[dependencies]
deck_gen_wasm_conf = { path = "../conf" }
web-sys = { version = "0.3", features = ["Window", "Location"] }
