# Что сделать с вашей стороны

1. Пересобрать WASM (`start.bat` / `trunk serve`) и обновить страницу.
2. В explorer у `.jpg` / `.png` / `.icon` (и `.jpeg` / `.ico`) слева от имени должна быть бирюзовая иконка-файл с пейзажем, не пустой слот.
3. Браузер я не открывал — только unit-тесты и wasm32 check.
