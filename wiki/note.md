# Пост-действия (требуются от пользователя)

После применения изменений по todo.md (текущая задача: *.j2 загрузка + синтаксис js/j2):

1. Ручная проверка в браузере (обязательно, т.к. unit-тесты не заменяют реальный File Picker и не показывают реальную подсветку):
   - `serve.bat` (или `trunk serve`)
   - Открыть http://127.0.0.1:8080
   - Загрузить папку, содержащую .j2 (например, скопировать games/monopoly-2.0/views/face-layout.j2 в новую игру или использовать существующие views после load).
   - Загрузить .js напрямую или из папки.
   - Открыть .js файл в редакторе — должна быть цветная подсветка (не plain textarea). Открыть .j2 — либо подсветка (если syntect знает "j2"), либо plain (graceful).
   - Проверить, что .j2 и .js видны в explorer (как обычные файлы, без специальной иконки — это ок).
   - Проверить отклонение других расширений.
   - Desktop + (при наличии) mobile viewport.
   - Проверить consistency: после load, edit, save, экспорт — файлы остаются.

2. Полноценная иконка/классификация для .js и .j2 (ExplorerIcon, FileKind, preview и т.д.):
   - Можно сделать позже (добавив варианты в enum'ы fs), но не требовалось текущей задачей. Сейчас они Other + syntax для highlight/editor.

3. Полная проверка (опционально):
   - cargo test -p deck_gen_wasm_fs -p deck_gen_wasm_import -p deck_gen_wasm_ui
   - trunk build (или serve)
   - (вне инструкции) можно проверить и deck_gen CLI, но анализ/правки были только по deck_gen_wasm.

4. Зафиксировать изменения git'ом (пример):
   - git add deck_gen_wasm/conf/src/api.rs deck_gen_wasm/fs/src/file_kind.rs deck_gen_wasm/import/src/policy.rs deck_gen_wasm/ui/src/windows/editor/highlight.rs deck_gen_wasm/ui/Cargo.toml wiki/result.md wiki/note.md

5. Если в будущем добавлять другие текстовые типы ( .ts, .mdx и т.п.) — одна строка в IMPORT_TEXT + тест в policy/fs + (при необходимости) строка в syntax_name.

Никаких других действий не требуется от пользователя для завершения этой задачи. Изменения изолированы в deck_gen_wasm*, код простой, тесты+build+trunk-build подтверждены.

# Пост-действия по interface_scale_support.md (добавлены после решения)

После применения правок (только deck_gen_wasm/style.css):

1. Обязательная ручная верификация в браузере (по правилам пользователя + приёмке в interface_scale_support.md; unit-тесты и trunk build не заменяют resize + взаимодействие):
   - `serve.bat` (или `trunk serve`)
   - Открыть http://127.0.0.1:8080 (или порт по умолчанию)
   - Сузить окно браузера по вертикали до высоты ~350-420px (имитировать низкую вкладку):
     - Убедиться: вертикальный scrollbar на странице/ body / html отсутствует.
     - Убедиться: все кнопки Activity Bar (включая нижнюю Clear) видны, нажимаются, без скролла самой полосы activity-bar.
   - Раскрыть папку с большим числом файлов (например, если есть, или создать несколько в games/):
     - Скроллбар появляется ТОЛЬКО внутри .tree .
     - Заголовок "Games", кнопки New File / New Folder, строка статуса — остаются на месте (не уходят при скролле дерева).
   - Проверить split-превью: обе половины редактора (если включен split) заполняют доступную высоту справа от explorer; iframe / content не ломается.
   - Расширить окно до обычного desktop: кнопки ~40×40, spacer работает, Clear прижат к низу, визуально как раньше.
   - Проверить на desktop + (если можно) эмулировать mobile viewport.
   - Edge-кейсы: пустой workspace, loading state, модалки поверх (не влияют на layout).

2. Если при ручной проверке что-то не так (редко):
   - Убедиться, что 100dvh и min-height:0 применяются (devtools → elements → computed).
   - При очень маленьких высотах кнопки сжимаются до ~20px (это ок по min-height).

3. Cargo / check (уже выполнены, но повторить после любых правок):
   - cargo test -p deck_gen_wasm_ui --lib
   - cargo check -p deck_gen_wasm
   - trunk build

4. Git (пример для коммита):
   - git add deck_gen_wasm/style.css wiki/result.md wiki/note.md
   - (никакие другие файлы по инструкции "прочие игнорируй" и "минимальные изменения" не менялись)

Никаких изменений архитектуры, разметки Leptos, добавления контролов масштаба или внутренних скроллов на activity-bar — не было. Задача решена на этапе-1 минимально.

## Пост-действия после flixible_icons (PNG activity buttons)

1. Ручная браузерная проверка (обязательна, т.к. нет автоматизации в сессии):
   - `trunk serve` (или serve.bat)
   - Открыть http://127.0.0.1:8080
   - Навести курсор на все кнопки Activity Bar (слева) — иконка должна меняться (off → on).
   - Зажать мышкой (mousedown) — должен показываться click кадр.
   - Для Split Preview: кликнуть — должна появиться dedicated active иконка (не просто цвет рамки слева); при наведении на активную — on; при зажатии — click.
   - Убедиться, что Save/Download/Load/New/Html/Pdf/Clear/Split все используют PNG, ни одна не осталась на SVG.
   - Проверить, что правка любого off.drawio.png + повторный trunk build сразу отражается (без правки rs).
   - Проверить disabled (load) — иконка тусклая через существующую opacity на .activity-btn:disabled.
   - Desktop (основной) + по возможности узкий viewport.

2. Качество иконок (опционально сейчас):
   - Текущие PNG нарисованы простым кодом (System.Drawing) только чтобы был механизм. Если нужно красивее/точнее — заменить файлы на экспорты из draw.io (имена и размеры 32×32 с прозрачным фоном сохранить).
   - При желании можно увеличить display size до 20-24px или подкрутить padding в CSS, но это за рамками минимальной задачи.

3. SaveButton:
   - Save всё ещё присутствует в коде (и в icons). Если параллельно выполняется remove_save_button — после удаления кнопки можно будет удалить и папку save/ (но не обязательно для этой задачи).

4. Полная проверка:
   - cargo test -p deck_gen_wasm_ui --lib
   - cargo check -p deck_gen_wasm
   - trunk build (проверить dist/icons/buttons)

Никаких архитектурных изменений не вносилось. Дальше можно удалять старые SVG если где-то остались (в этой задаче — нет).
