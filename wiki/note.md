# Note — remove_game_prefix_from_deck (2026-09-17)

## Выполненные доработки
- В catalog::load_deck ослаблена проверка `name` из data.json5: теперь принимает как полное "game.rel", так и короткое "rel" (без префикса игры).
- Полное имя всегда берётся из пути (dotted_name), Deck.name канонизируется к полному (чтобы game_for_deck_name / output_dir / labels работали без изменений).
- Изменения строго минимальны (один if в приватной fn), архитектура не тронута.
- Этап-1: простой прямолинейный код + верификация.
- Этап-2: рефак (упростил до split_once, обновил точные доки, KISS, стиль).
- Сборка + тесты deck_gen — зелёные.

## Что проверено
- cargo test -p deck_gen : 8 passed.
- cargo build -p deck_gen : OK.
- Короткие имена без game. в `name` data.json5 теперь работают (и старые полные тоже); mismatch по-прежнему ошибка.
- Полные имена везде (queries, файлы вывода, internal) — без регрессий.

## Требуемые действия от пользователя
Действий не требуется. Задача решена в deck_gen (минимально, по всем правилам). Можно коммитить.

(Предыдущие записи очищены перед новой.)
