# Задача
Необходимо проанализировать код крейта `deck_gen_wasm` и скорректировать задачи 3-ей фазы работ, сформулированных в отдельных файлах в `./wiki/plan/phase-III/stage-2`.

Цель корректировки:
1. уточнение формулировок для максимальной конкретизации поставленной задачи;
2. определение и ограничение минимально необходимой рабочей области кода для анализа Grok AI, достаточной для качественного решения поставленной задачи;
3. если какие-то задачи пересекаются по изменениям разрешено сформулировать единый scope в виде отдельной задачи в новом файле в `./wiki/plan/phase-III/stage-2`.

## Выполнено (2026-09-15)
- Проанализирован крейт `deck_gen_wasm` (ui/, workspace/, conf/, style.css, раскладка в app.rs, Editor/Explorer/EditorTab, split, DelayedTooltip, ContextMenu и т.д.).
- Уточнены и конкретизированы все 5 задач stage-2:
  - Добавлены разделы «Текущее состояние», «Что сделать», «Не делать», «Приёмка».
  - Для каждой указан **минимальный** список файлов/папок для анализа (в разделе "Какой код использовать для анализа").
- Обнаружено пересечение close-all + rearrange по коду табов → создана единая `tabs_management.md` со сводным скоупом.
- Обновлены ссылки и список в `project-development-plan.md`.
- Старые файлы close_all... и open_tabs_rearrange сведены к редиректам на объединённый.
- Фиксированы имена файлов (опечатки в названиях).

Все цели корректировки достигнуты. Дальнейшая реализация фич — по обновлённым описаниям в stage-2/.
