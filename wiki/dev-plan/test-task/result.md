DefaultTools.verbosity_help: заглушка `...` → читает `tool_help.md` рядом с модулем и возвращает текст (метод должен отдавать подробную справку, а не Ellipsis).

Tools/harness/agents/tools/tool_help.md: пустой файл → описание каждого инструмента DefaultTools с параметрами, поведением, ошибками и примерами (справка должна жить в md, а не в коде метода).
